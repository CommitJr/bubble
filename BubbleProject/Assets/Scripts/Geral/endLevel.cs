using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class endLevel : MonoBehaviour
{
    // script que congela o controle da bolha quando ela atinge a barra 2
    // a bolha deve se mover para o centro e depois subir, passando para o proximo nivel
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
